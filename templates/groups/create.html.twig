{% extends '/base.html.twig' %}

{% block title  %} new Group {% endblock %}

{% block body %}
    <a href="/group/list" class="btn btn-dark">back</a>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{form_end(form)}}
{% endblock %}
